source "https://rubygems.org"

# Matches the GitHub Pages build environment for local preview
gem "github-pages", group: :jekyll_plugins

# Required for remote theme support
gem "jekyll-remote-theme"
